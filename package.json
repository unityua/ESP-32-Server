{
  "name": "esp32-led-server",
  "version": "2.0.0",
  "description": "Hardened ESP32 LED control server (Render edition)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "gen:secret": "node -e \"console.log(require('crypto').randomBytes(32).toString('hex'))\"",
    "gen:seed": "node scripts/make-seed.js"
  },
  "engines": { "node": "22.x" },
  "dependencies": {
    "express": "^4.19.2",
    "helmet": "^7.1.0",
    "express-rate-limit": "^7.2.0",
    "better-sqlite3": "^11.0.0",
    "bcryptjs": "^2.4.3"
  }
}
